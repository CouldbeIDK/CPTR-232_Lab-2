To compile just use the good ol' "make" then the filename

When the time comes to run the program run it as "./InsertsionSort <FILENAME.txt >FILE2.txt" with FILENAME being the name of the input file, and FILE2 being the outputfile